{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the JARVIS main window. The dialog plugin is the only way a new folder becomes readable; there is no filesystem or HTTP capability, because the engine — not the webview — touches the disk.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open"
  ]
}
