# jarvis-engine

Motor de conocimiento de JARVIS. Paquete Python puro: sin Tauri, sin Node, sin
red y sin dependencias externas en tiempo de ejecución.

```bash
pip install -e ".[dev]"
python -m pytest tests
python -m jarvis_engine.cli --help
```

La arquitectura está descrita en `../ARCHITECTURE.md`.
